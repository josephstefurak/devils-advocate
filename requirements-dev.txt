# pytest, pytest-asyncio: pinned in backend/requirements.txt (install with -r backend/requirements.txt first)
pytest-mock==3.14.0
httpx==0.28.1          # already in requirements.txt, needed for ASGI test client
python-socketio[asyncio_client]==5.16.1  # for integration tests